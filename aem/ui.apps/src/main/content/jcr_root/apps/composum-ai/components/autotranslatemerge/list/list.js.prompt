Verify whether the javascript file is consistent with the specification and if not update it accordingly,
while minimizing changes.

The javascript should contain a class AITranslateMergeTool that handles the general script functionality and a class AITranslateMergeRow for each row in the list.

